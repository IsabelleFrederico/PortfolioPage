import { Section } from "../Interface"
import { motion } from "motion/react"

export const ProjectsSection = () => {
    return (
        <Section>
            <div className="w-full pb-120">
                <h2 className="text-5xl font-bold">Projects</h2>
            </div>
        </Section>
    )
}